import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Remove <React.StrictMode> wrapper if necessary
ReactDOM.render(
  <App />,
  document.getElementById("root")
);
